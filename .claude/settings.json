{
  "permissions": {
    "allow": []
  },
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "cd /c/Users/I750407/ltvf-dashboard && git add -A && git diff --cached --quiet || git commit -m \"auto: update $(git diff --cached --name-only | tr '\\n' ' ')\"",
            "timeout": 30
          }
        ]
      }
    ]
  }
}
