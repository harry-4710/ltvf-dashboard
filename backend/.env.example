# SAP Connection — fill in after BASIS team responds
SAP_BASE_URL=https://<sap-host>:<port>
SAP_CLIENT=100
SAP_USER=<api-service-user>
SAP_PASSWORD=<api-service-password>
SAP_ODATA_SERVICE=/sap/opu/odata/sap/CNVLTVF3_SRV

# Set to "true" to use live SAP, "false" to use mock data
USE_MOCK=true
